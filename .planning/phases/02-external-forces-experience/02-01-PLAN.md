---
phase: 02-external-forces-experience
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - vitest.config.ts
  - tests/useExternalForces.test.ts
  - tests/ExternalForcesPanel.test.tsx
  - package.json
autonomous: true
requirements: [API-01, API-02, API-03, LOG-01, LOG-02, LOG-03, LOG-04, LOG-05, LOG-06]

must_haves:
  truths:
    - "Running npx vitest run exits without configuration errors"
    - "tests/useExternalForces.test.ts exists with describe blocks for API-01, API-02, API-03"
    - "tests/ExternalForcesPanel.test.tsx exists with describe blocks for LOG-01 through LOG-06"
    - "All test stubs fail (RED) — no implementation yet, so assertions fail by design"
  artifacts:
    - path: "vitest.config.ts"
      provides: "Vitest config with jsdom environment and @testing-library/jest-dom setup"
    - path: "tests/useExternalForces.test.ts"
      provides: "Test stubs for hook state machine (API-01, API-02, API-03)"
    - path: "tests/ExternalForcesPanel.test.tsx"
      provides: "Test stubs for panel rendering and interactions (LOG-01 through LOG-06)"
  key_links:
    - from: "vitest.config.ts"
      to: "tests/*.test.ts(x)"
      via: "test environment: jsdom"
      pattern: "environment.*jsdom"
    - from: "tests/ExternalForcesPanel.test.tsx"
      to: "@testing-library/react"
      via: "import { render, screen }"
      pattern: "from '@testing-library/react'"
---

<objective>
Install the test framework and create failing test stubs for every automated requirement in Phase 2.

Purpose: Nyquist compliance — every automated task in waves 2 and 3 has a pre-existing test file to run against. Wave 0 stubs go RED now; implementation turns them GREEN.
Output: vitest.config.ts, two test files with failing assertions, devDependencies installed.
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-external-forces-experience/02-CONTEXT.md
@.planning/phases/02-external-forces-experience/02-RESEARCH.md
@.planning/phases/02-external-forces-experience/02-VALIDATION.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Install test framework and create vitest config</name>
  <files>package.json, vitest.config.ts</files>
  <action>
Run: `npm install -D vitest @testing-library/react @testing-library/user-event @testing-library/jest-dom jsdom msw`

Create vitest.config.ts at project root:

```ts
import { defineConfig } from 'vitest/config'
import react from '@vitejs/plugin-react'

export default defineConfig({
  plugins: [react()],
  test: {
    environment: 'jsdom',
    globals: true,
    setupFiles: ['./tests/setup.ts'],
  },
})
```

Create tests/setup.ts:
```ts
import '@testing-library/jest-dom'
```

Create tests/ directory if absent. Verify: `npx vitest run` exits without "config not found" or import errors (test files don't exist yet, so it will report "no test files found" — that is acceptable at this step).
  </action>
  <verify>
    <automated>npx vitest run --reporter=verbose 2>&1 | grep -E "no test files|RUN|PASS|FAIL|error" | head -20</automated>
  </verify>
  <done>vitest.config.ts exists, tests/setup.ts exists, npm ls vitest shows version installed</done>
</task>

<task type="auto">
  <name>Task 2: Create failing test stubs for hook and panel</name>
  <files>tests/useExternalForces.test.ts, tests/ExternalForcesPanel.test.tsx</files>
  <action>
Create tests/useExternalForces.test.ts — covers API-01, API-02, API-03. Import the hook once it exists (the file will fail to import until Plan 02 creates the hook — that is the intended RED state):

```ts
import { describe, it, expect, vi, beforeEach } from 'vitest'
// Hook does not exist yet — this file fails at import. That is correct RED state.
// import { useExternalForces } from '../src/hooks/useExternalForces'

describe('useExternalForces — API-01: on-demand only', () => {
  it('does not fetch on mount — trigger must be called explicitly', () => {
    // Stub: will implement after hook exists
    expect(true).toBe(false) // intentional RED
  })
})

describe('useExternalForces — API-02: session cache', () => {
  it('second trigger for same weekIdx uses cached events, fetch not called again', () => {
    expect(true).toBe(false) // intentional RED
  })
})

describe('useExternalForces — API-03: graceful failure', () => {
  it('all-failure returns error status', () => {
    expect(true).toBe(false) // intentional RED
  })

  it('partial failure (some days succeed) returns loaded status with partial pool', () => {
    expect(true).toBe(false) // intentional RED
  })
})
```

Create tests/ExternalForcesPanel.test.tsx — covers LOG-01 through LOG-06:

```tsx
import { describe, it, expect } from 'vitest'
// Component does not exist yet — intentional RED
// import { ExternalForcesPanel } from '../src/components/sheet/ExternalForcesPanel'

describe('ExternalForcesPanel — LOG-01: renders in note step', () => {
  it('panel is present in the DOM when logStep is note', () => {
    expect(true).toBe(false) // intentional RED
  })
})

describe('ExternalForcesPanel — LOG-02: event display format', () => {
  it('renders {year} — {text} in muted italic after trigger', () => {
    expect(true).toBe(false) // intentional RED
  })
})

describe('ExternalForcesPanel — LOG-03: action buttons after load', () => {
  it('shows Show Next and Add to Record buttons when status is loaded', () => {
    expect(true).toBe(false) // intentional RED
  })
})

describe('ExternalForcesPanel — LOG-04: save action', () => {
  it('Add to Record calls setExternalForce with correct weekKey and force object', () => {
    expect(true).toBe(false) // intentional RED
  })
})

describe('ExternalForcesPanel — LOG-05: saved block is separate', () => {
  it('saved block is labeled Signal and is not merged with note textarea', () => {
    expect(true).toBe(false) // intentional RED
  })
})

describe('ExternalForcesPanel — LOG-06: inline editing', () => {
  it('editing Signal textarea calls updateExternalForceText (debounced)', () => {
    expect(true).toBe(false) // intentional RED
  })
})
```
  </action>
  <verify>
    <automated>npx vitest run tests/useExternalForces.test.ts tests/ExternalForcesPanel.test.tsx --reporter=verbose 2>&1 | tail -20</automated>
  </verify>
  <done>Both test files exist. npx vitest run reports failures (RED) — not "no test files". No configuration errors. Test count ≥ 8 failing tests reported.</done>
</task>

</tasks>

<verification>
npx vitest run 2>&1 | tail -10
</verification>

<success_criteria>
- vitest, @testing-library/react, @testing-library/user-event, @testing-library/jest-dom, jsdom, msw all listed in package.json devDependencies
- vitest.config.ts exists with jsdom environment and setupFiles
- tests/useExternalForces.test.ts has 4 failing test cases (API-01, API-02, API-03 x2)
- tests/ExternalForcesPanel.test.tsx has 6 failing test cases (LOG-01 through LOG-06)
- npx vitest run runs without configuration errors — only test failures (RED is correct)
</success_criteria>

<output>
After completion, create `.planning/phases/02-external-forces-experience/02-01-SUMMARY.md`
</output>
